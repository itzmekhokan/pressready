# The image installs PressReady from Packagist (see Dockerfile), so the build
# context only needs the Dockerfile + entrypoint.sh. Keep everything else out so
# the build context stays tiny and image builds stay fast.
*
!entrypoint.sh
